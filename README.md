# VSCode Docker-Compose volume pathes bug

## Reproducing:


1. Via running `docker-compose up`, **docker** folder appears in **root**(`./docker/...)
2. Via running with devcontainer,  **docker** folder appears in **.devcontainer**(`./.devcontainer/docker/...)